{
  "name": "stylelint",
  "version": "1.0.0",
  "scripts": {
    "build": "parcel build src/*.html --dist-dir dist"
  },
  "devDependencies": {
    "htmlhint": "^1.1.4",
    "parcel": "^2.12.0",
    "stylelint": "^16.9.0",
    "stylelint-config-standard": "^36.0.0"
  }
}